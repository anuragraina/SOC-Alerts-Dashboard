{
  "name": "soc-alerts-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "SOC Alerts Dashboard — single-user security alerts monitoring",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "cd backend && node dist/index.js"
  }
}
